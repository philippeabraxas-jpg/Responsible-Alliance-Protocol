# OPA Secure Configuration
# Hardens OPA instance against unauthorized access

# Decision logging (important for TBP)
decision_logs:
  console: true

# Security settings
services:
  # In production, this would point to a controller
  # controller:
  #   url: https://tbp-controller.internal

# Status reporting
status:
  console: true

# Encryption at rest (handled by volume encryption in production)
# TLS configuration (uncomment for production)
# keys:
#   ca: /etc/opa/ca.crt
#   cert: /etc/opa/tls.crt
#   key: /etc/opa/tls.key
